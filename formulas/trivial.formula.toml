description = "Single-step workflow for obvious fixes, config changes, typos, and documentation updates. The bead IS the spec. No design phase, no review gate."
formula = "trivial"
type = "workflow"
version = 1

[[steps]]
id = "implement"
title = "Fix {{issue}}"
description = "Implement the fix described in the bead. This is a trivial change — no design needed, no ambiguity. Read the bead, make the change, verify it works."
checklist = "impl-ready"

[[steps]]
id = "submit"
title = "Submit for merge"
description = "Submit per /seed-pr-merge. Push branch, open PR via gh CLI with title 'type(scope): summary (bead-id)', wait for CI, squash-merge. Triage level 'auto' means CI is the gate, not human review."
needs = ["implement"]
checklist = "merge-ready"

[vars]
[vars.issue]
description = "What needs fixing"
required = true
